# CampusConnect Frontend Environment Configuration - FIXED FOR CREDENTIALS
# Copy this file to .env and update the values as needed

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Appwrite Configuration  
VITE_APPWRITE_PROJECT_ID=your-appwrite-project-id
VITE_APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1

# API Configuration - IMPORTANT: Use consistent localhost/127.0.0.1
VITE_API_BASE_URL=http://localhost:8000

# Development Configuration
VITE_FRONTEND_URL=http://localhost:3000

# CORS & Credentials Fix:
# - Frontend and backend must use the same hostname (localhost OR 127.0.0.1)
# - Cannot use allow_origins="*" when withCredentials=true
# - Backend now allows specific localhost origins for credentials support
